# Security

Report uploads can contain sensitive infrastructure metadata. Configure `FLEET_TOKEN`, bind to a private interface, place TLS at a trusted reverse proxy, and restrict access to the data directory. Report vulnerabilities privately through GitHub Security Advisories.
