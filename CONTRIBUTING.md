# Contributing

Run `gofmt -w .`, `go test ./...`, and `go vet ./...` before opening a pull request. Keep the HTTP contract backward compatible and include tests for persistence, authentication, and path validation changes.
